# Diagnostic PTA method defaults — inherits shared base.

defaults:
  - ../base

patch_level:
  conf_margin_threshold: 0.00
  aug_copies: 0
  patch_filter_mode: none

fusion:
  tau_image_proto: 80.0
  mode: patch_only           # patch_only | image_only
  quality_modulation: 1.0
  alpha_gate_scale: 0.5
